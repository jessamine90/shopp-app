<h3>Sklepik z marzeniami</h3>

Kategorie:
<table>
	<% @categories.each do |category| %>
		<tr>
			<td><%= link_to "#{category.name}", category %></td>
		</tr>
	<% end %>
</table>
</p>
Produkty:
<table>
<% @products.each do |product| %>
	<tr>
		<td><%= link_to "#{product.name}", product %></td>
	</tr>
<% end %>
</table>
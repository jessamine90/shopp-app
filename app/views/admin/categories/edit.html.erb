<h2>Edytuj kategorie</h2>

<%= form_for(@category, :url => admin_category_path(@category)) do |f| %>
	<div class="field">
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>

<p><%= link_to 'powrot', admin_categories_path %>


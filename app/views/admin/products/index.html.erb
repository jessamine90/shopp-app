<h1>Produkty</h1>

<table>
	<tr>
		<th>nazwa</th>
		<th>cena</th>
		<th>kategoria</th>
		<th></th>
		<th></th>
		<th></th>
 	</tr>
	<% @products.each do |product| %>
 	<tr>
		<td><%= product.name %></td>
		<td><%= product.price %></td>
		<td><%= product.category_id %></td>
		<td><%= link_to 'edytuj', edit_admin_product_path(product) %></td>
		<td><%= link_to 'usun', admin_product_path(product), confirm: 'Jestes pewien?', method: :delete %></td>
		</tr>
	<% end %>
</table>
<br />

<%= link_to 'dodaj produkt', new_admin_product_path %>


<p><%= link_to 'powrot', :admin_root %></p>